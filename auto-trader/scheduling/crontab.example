# auto-trader 定时任务示例（Linux/cron）。
# macOS 建议用 launchd（更可靠、休眠唤醒后会补跑）——见 com.user.autotrader.plist.example。
#
# 安装方式: crontab -e，把下面这行粘进去，并把路径换成你本机的绝对路径。
# 下面示例是"交易时段内每 15 分钟跑一次"，具体时间按你交易的市场时区调整
# （美股/港股/A股的开收盘时间不一样，这里的 9-16 只是占位，不要直接照抄）。
#
# 依赖只有 python3 + pip 装的 longport 包，没有额外的二进制/PATH 要求。

*/15 9-16 * * 1-5 cd /ABSOLUTE/PATH/TO/ai-quant/trading-analyst/auto-trader && /usr/bin/python3 scripts/run_cycle.py >> logs/cron.log 2>&1
